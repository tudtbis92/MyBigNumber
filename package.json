{
  "name": "mybignumber",
  "version": "0.0.2",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "demo": "npm run build && node dist/demo.js",
    "test": "npm run build && node --test tests/MyBigNumber.test.mjs tests/server.test.mjs",
    "coverage": "npm run build && c8 --reporter=lcov --reporter=text node --test tests/MyBigNumber.test.mjs tests/server.test.mjs",
    "serve": "npm run build && node dist/serve.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/node": "^22.20.3",
    "c8": "^12.0.0",
    "typescript": "^7.0.2"
  }
}
